import{BadRequestException,ConflictException,HttpException,HttpStatus,Injectable,UnauthorizedException}from'@nestjs/common';import{InjectRepository}from'@nestjs/typeorm';import{DataSource,Repository}from'typeorm';import{JwtService}from'@nestjs/jwt';import*as bcrypt from'bcrypt';import{randomInt}from'crypto';import{User}from'../entities/user.entity';import{EmailVerification}from'../entities/email-verification.entity';import{MailDispatcher}from'../mail/mail-dispatcher';
const EXPIRES_MINUTES=5,RESEND_SECONDS=60,MAX_ATTEMPTS=5;
@Injectable()export class AuthService{constructor(@InjectRepository(User)private users:Repository<User>,@InjectRepository(EmailVerification)private verifications:Repository<EmailVerification>,private jwt:JwtService,private mail:MailDispatcher,private db:DataSource){}
async requestRegistration(email:string,password:string,name:string){email=email.trim().toLowerCase();if(await this.users.findOneBy({email}))throw new ConflictException('이미 가입된 이메일입니다.');const previous=await this.verifications.findOneBy({email});if(previous&&Date.now()-previous.lastSentAt.getTime()<RESEND_SECONDS*1000)throw new HttpException('인증번호는 60초 후 다시 요청할 수 있습니다.',HttpStatus.TOO_MANY_REQUESTS);const code=this.createCode(),now=new Date(),expiresAt=new Date(now.getTime()+EXPIRES_MINUTES*60000);const entity=this.verifications.create({...(previous??{}),email,name,passwordHash:await bcrypt.hash(password,12),verificationCodeHash:await bcrypt.hash(code,10),expiresAt,attemptCount:0,sendCount:(previous?.sendCount??0)+1,lastSentAt:now,verifiedAt:null});await this.verifications.save(entity);await this.mail.sendVerification({to:email,code,expiresInMinutes:EXPIRES_MINUTES});return{email,expiresInSeconds:EXPIRES_MINUTES*60,resendAfterSeconds:RESEND_SECONDS}}
async resendVerification(email:string){email=email.trim().toLowerCase();const pending=await this.verifications.createQueryBuilder('v').addSelect('v.verificationCodeHash').where('v.email=:email',{email}).getOne();if(!pending)throw new BadRequestException('먼저 회원가입 정보를 입력해주세요.');if(Date.now()-pending.lastSentAt.getTime()<RESEND_SECONDS*1000)throw new HttpException('인증번호는 60초 후 다시 요청할 수 있습니다.',HttpStatus.TOO_MANY_REQUESTS);const code=this.createCode();pending.verificationCodeHash=await bcrypt.hash(code,10);pending.expiresAt=new Date(Date.now()+EXPIRES_MINUTES*60000);pending.lastSentAt=new Date();pending.attemptCount=0;pending.sendCount++;await this.verifications.save(pending);await this.mail.sendVerification({to:email,code,expiresInMinutes:EXPIRES_MINUTES});return{email,expiresInSeconds:EXPIRES_MINUTES*60,resendAfterSeconds:RESEND_SECONDS}}
async verifyRegistration(email:string,code:string){email=email.trim().toLowerCase();const pending=await this.verifications.createQueryBuilder('v').addSelect(['v.passwordHash','v.verificationCodeHash']).where('v.email=:email',{email}).getOne();if(!pending)throw new BadRequestException('인증 요청을 찾을 수 없습니다.');if(pending.expiresAt.getTime()<Date.now())throw new BadRequestException('인증번호가 만료되었습니다. 다시 발급해주세요.');if(pending.attemptCount>=MAX_ATTEMPTS)throw new BadRequestException('입력 횟수를 초과했습니다. 인증번호를 다시 발급해주세요.');if(!await bcrypt.compare(code,pending.verificationCodeHash)){pending.attemptCount++;await this.verifications.save(pending);throw new BadRequestException(`인증번호가 올바르지 않습니다. (${MAX_ATTEMPTS-pending.attemptCount}회 남음)`)}const user=await this.db.transaction(async em=>{if(await em.findOneBy(User,{email}))throw new ConflictException('이미 가입된 이메일입니다.');const created=await em.save(User,em.create(User,{email,name:pending.name,passwordHash:pending.passwordHash}));await em.delete(EmailVerification,{id:pending.id});return created});return this.issue(user)}
async login(email:string,password:string){const user=await this.users.createQueryBuilder('u').addSelect('u.passwordHash').where('u.email = :email',{email:email.trim().toLowerCase()}).getOne();if(!user?.passwordHash||!await bcrypt.compare(password,user.passwordHash))throw new UnauthorizedException('이메일 또는 비밀번호를 확인해주세요.');return this.issue(user)}private createCode(){return String(randomInt(0,1000000)).padStart(6,'0')}private issue(user:User){return{accessToken:this.jwt.sign({sub:user.id,email:user.email}),user:{id:user.id,email:user.email,name:user.name}}}}
