import type{PaymentMethod}from'./types';const base=process.env.NEXT_PUBLIC_API_URL??'http://localhost:4000/api';async function request<T>(path:string,init?:RequestInit){const token=localStorage.getItem('budget-token'),response=await fetch(`${base}${path}`,{...init,headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`,...init?.headers}}),body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(Array.isArray(body.message)?body.message[0]:body.message??'요청에 실패했습니다.');return body as T}export type PaymentMethodPayload=Omit<PaymentMethod,'id'|'createdAt'>;export const paymentMethodApi={create:(body:PaymentMethodPayload)=>request<PaymentMethod>('/payment-methods',{method:'POST',body:JSON.stringify(body)}),update:(id:string,body:PaymentMethodPayload)=>request<PaymentMethod>(`/payment-methods/${id}`,{method:'PATCH',body:JSON.stringify(body)}),remove:(id:string,workspaceId:string)=>request(`/payment-methods/${id}?workspaceId=${workspaceId}`,{method:'DELETE'})};
