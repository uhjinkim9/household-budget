'use client';import{useEffect,useState,type ReactNode}from'react';import{useRouter}from'next/navigation';import{hasSession}from'@/lib/auth';import{LogoutButton}from'@/components/auth/LogoutButton';import{SettingsNavigation}from'@/components/settings/SettingsNavigation';import s from'../route-loading.module.scss';export default function SettingsLayout({children}:{children:ReactNode}){const router=useRouter(),[ready,setReady]=useState(false);useEffect(()=>{if(!hasSession())router.replace('/login');else setReady(true)},[router]);return ready?<><SettingsNavigation/><LogoutButton/>{children}</>:<main className={s.loading}>로그인 정보를 확인하고 있어요…</main>}
