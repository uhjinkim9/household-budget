'use client';import{useState,type InputHTMLAttributes}from'react';import{Input}from'./Input';type Props=Omit<InputHTMLAttributes<HTMLInputElement>,'type'|'name'|'value'|'defaultValue'|'onChange'>&{name:string;defaultValue?:number|string|null;value?:number|null;onValueChange?:(value:number|null)=>void;allowNegative?:boolean};export function MoneyInput({name,defaultValue,value,onValueChange,allowNegative=false,placeholder='0',...props}:Props){const controlled=value!==undefined,[internal,setInternal]=useState<number|null>(()=>normalize(defaultValue)),[negativeDraft,setNegativeDraft]=useState(false),current=controlled?value:internal;function change(text:string){const compact=text.replace(/,/g,'').trim();if(allowNegative&&compact==='-'){setNegativeDraft(true);if(!controlled)setInternal(null);onValueChange?.(null);return}setNegativeDraft(false);const digits=compact.replace(/[^0-9]/g,'');const negative=allowNegative&&compact.startsWith('-');const next=digits===''?null:Number(`${negative?'-':''}${digits}`);if(!controlled)setInternal(next);onValueChange?.(next)}return <><Input {...props} type="text" inputMode={allowNegative?'text':'numeric'} autoComplete="off" value={negativeDraft?'-':current===null?'':current.toLocaleString('ko-KR')} onChange={event=>change(event.target.value)} placeholder={placeholder}/><input type="hidden" name={name} value={current??''}/></>}function normalize(value:number|string|null|undefined){if(value===null||value===undefined||value==='')return null;const number=Number(String(value).replace(/,/g,''));return Number.isFinite(number)?Math.trunc(number):null}
