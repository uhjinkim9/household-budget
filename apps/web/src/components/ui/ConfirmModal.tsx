import{Modal}from'./Modal';import{Button}from'./Button';import s from'./ConfirmModal.module.scss';export function ConfirmModal({open,title,description,confirmLabel='확인',loading=false,danger=false,onConfirm,onClose}:{open:boolean;title:string;description:string;confirmLabel?:string;loading?:boolean;danger?:boolean;onConfirm:()=>void|Promise<void>;onClose:()=>void}){return <Modal open={open} title={title} onClose={onClose}><div className={s.content}><p>{description}</p><div><Button type="button" variant="ghost" onClick={onClose} disabled={loading}>취소</Button><Button type="button" variant={danger?'danger':'primary'} onClick={onConfirm} disabled={loading}>{loading?'처리 중…':confirmLabel}</Button></div></div></Modal>}
