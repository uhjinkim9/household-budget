import{Modal}from'@/components/ui/Modal';import type{WorkspaceMember}from'@/lib/workspace-settings-api';import s from'./MemberProfileModal.module.scss';const roleLabel={OWNER:'가계 관리자',MEMBER:'구성원',VIEWER:'조회자'};export function MemberProfileModal({member,onClose}:{member:WorkspaceMember|null;onClose:()=>void}){if(!member)return null;return <Modal open title="구성원 프로필" onClose={onClose}><div className={s.profile}><div className={s.avatar}>{member.profileImageUrl?<img src={member.profileImageUrl} alt={`${member.name} 프로필`}/>:<span>{member.name.slice(0,1)}</span>}</div><h2>{member.name}</h2><span className={s.role}>{roleLabel[member.role]}</span><dl><div><dt>이메일</dt><dd>{member.email}</dd></div><div><dt>권한</dt><dd>{roleLabel[member.role]}</dd></div><div><dt>참여일</dt><dd>{new Date(member.joinedAt).toLocaleDateString('ko-KR')}</dd></div></dl></div></Modal>}
